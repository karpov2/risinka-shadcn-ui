/**
 * @section Marker
 * @title Markers
 */
import { MarkerExample } from '../../../../src/examples/marker-example';
import { StateFrame } from '../../../library/kit';

/** Marker · Markers — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/marker-example.tsx, тег shadcn@4.21.0). */
export default function MarkerExampleState() {
  return (
    <StateFrame muted>
      <MarkerExample />
    </StateFrame>
  );
}
