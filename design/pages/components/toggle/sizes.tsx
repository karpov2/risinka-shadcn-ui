/**
 * @section Toggle
 * @title Sizes
 */
import { ToggleSizes } from '../../../../src/examples/toggle-example';
import { StateFrame } from '../../../library/kit';

/** Toggle · Sizes — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/toggle-example.tsx, тег shadcn@4.21.0). */
export default function ToggleSizesState() {
  return (
    <StateFrame muted>
      <ToggleSizes />
    </StateFrame>
  );
}
