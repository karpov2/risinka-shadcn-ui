/**
 * @section Поля и формы
 * @title Checkbox
 */
import CheckboxDemo from '../../../src/demos/checkbox-demo';
import { ComponentTile } from '../../library/kit';

/** Checkbox — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/checkbox-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function CheckboxTile() {
  return (
    <ComponentTile>
      <CheckboxDemo />
    </ComponentTile>
  );
}
