/**
 * @section Навигация
 * @title Tabs
 */
import { TabsDemo } from '../../../src/demos/tabs-demo';
import { ComponentTile } from '../../library/kit';

/** Tabs — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/tabs-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function TabsTile() {
  return (
    <ComponentTile>
      <TabsDemo />
    </ComponentTile>
  );
}
