/**
 * @section Dialog
 * @title No Close Button
 */
import { DialogNoCloseButton } from '../../../../src/examples/dialog-example';
import { StateFrame } from '../../../library/kit';

/** Dialog · No Close Button — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/dialog-example.tsx, тег shadcn@4.21.0). */
export default function DialogNoCloseButtonState() {
  return (
    <StateFrame muted>
      <DialogNoCloseButton />
    </StateFrame>
  );
}
