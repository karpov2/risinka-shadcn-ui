/**
 * @section Dialog
 * @title Chat Settings
 */
import { DialogChatSettings } from '../../../../src/examples/dialog-example';
import { StateFrame } from '../../../library/kit';

/** Dialog · Chat Settings — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/dialog-example.tsx, тег shadcn@4.21.0). */
export default function DialogChatSettingsState() {
  return (
    <StateFrame muted>
      <DialogChatSettings />
    </StateFrame>
  );
}
