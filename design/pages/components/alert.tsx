/**
 * @section Обратная связь
 * @title Alert
 */
import AlertDemo from '../../../src/demos/alert-demo';
import { ComponentTile } from '../../library/kit';

/** Alert — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/alert-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function AlertTile() {
  return (
    <ComponentTile>
      <AlertDemo />
    </ComponentTile>
  );
}
