/**
 * @section Toast
 * @title Promise
 */
import { ToastPromise } from '../../../../src/examples/toast-example';
import { StateFrame } from '../../../library/kit';

/** Toast · Promise — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/toast-example.tsx, тег shadcn@4.21.0). */
export default function ToastPromiseState() {
  return (
    <StateFrame muted>
      <ToastPromise />
    </StateFrame>
  );
}
