/**
 * @section Calendar
 * @title With Presets
 */
import { CalendarWithPresets } from '../../../../src/examples/calendar-example';
import { StateFrame } from '../../../library/kit';

/** Calendar · With Presets — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/calendar-example.tsx, тег shadcn@4.21.0). */
export default function CalendarWithPresetsState() {
  return (
    <StateFrame muted>
      <CalendarWithPresets />
    </StateFrame>
  );
}
