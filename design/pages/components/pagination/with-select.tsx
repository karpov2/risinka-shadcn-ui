/**
 * @section Pagination
 * @title With Select
 */
import { PaginationIconsOnly } from '../../../../src/examples/pagination-example';
import { StateFrame } from '../../../library/kit';

/** Pagination · With Select — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/pagination-example.tsx, тег shadcn@4.21.0). */
export default function PaginationIconsOnlyState() {
  return (
    <StateFrame muted>
      <PaginationIconsOnly />
    </StateFrame>
  );
}
