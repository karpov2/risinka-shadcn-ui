/**
 * @section Поля и формы
 * @title Field
 */
import FieldDemo from '../../../src/demos/field-demo';
import { ComponentTile } from '../../library/kit';

/** Field — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/field-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function FieldTile() {
  return (
    <ComponentTile>
      <FieldDemo />
    </ComponentTile>
  );
}
