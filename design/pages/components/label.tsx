/**
 * @section Поля и формы
 * @title Label
 */
import LabelDemo from '../../../src/demos/label-demo';
import { ComponentTile } from '../../library/kit';

/** Label — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/label-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function LabelTile() {
  return (
    <ComponentTile>
      <LabelDemo />
    </ComponentTile>
  );
}
