/**
 * @section Меню и окна
 * @title Popover
 */
import PopoverDemo from '../../../src/demos/popover-demo';
import { ComponentTile } from '../../library/kit';

/** Popover — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/popover-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function PopoverTile() {
  return (
    <ComponentTile>
      <PopoverDemo />
    </ComponentTile>
  );
}
