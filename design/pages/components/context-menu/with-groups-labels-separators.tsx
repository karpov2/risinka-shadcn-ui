/**
 * @section ContextMenu
 * @title With Groups, Labels & Separators
 */
import { ContextMenuWithGroups } from '../../../../src/examples/context-menu-example';
import { StateFrame } from '../../../library/kit';

/** ContextMenu · With Groups, Labels & Separators — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/context-menu-example.tsx, тег shadcn@4.21.0). */
export default function ContextMenuWithGroupsState() {
  return (
    <StateFrame muted>
      <ContextMenuWithGroups />
    </StateFrame>
  );
}
