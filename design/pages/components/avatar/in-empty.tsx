/**
 * @section Avatar
 * @title In Empty
 */
import { AvatarInEmpty } from '../../../../src/examples/avatar-example';
import { StateFrame } from '../../../library/kit';

/** Avatar · In Empty — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/avatar-example.tsx, тег shadcn@4.21.0). */
export default function AvatarInEmptyState() {
  return (
    <StateFrame muted>
      <AvatarInEmpty />
    </StateFrame>
  );
}
