/**
 * @section ResizablePanelGroup
 * @title With Handle
 */
import { ResizableWithHandle } from '../../../../src/examples/resizable-example';
import { StateFrame } from '../../../library/kit';

/** ResizablePanelGroup · With Handle — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/resizable-example.tsx, тег shadcn@4.21.0). */
export default function ResizableWithHandleState() {
  return (
    <StateFrame muted>
      <ResizableWithHandle />
    </StateFrame>
  );
}
