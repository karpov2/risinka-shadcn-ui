/**
 * @section ResizablePanelGroup
 * @title Controlled
 */
import { ResizableControlled } from '../../../../src/examples/resizable-example';
import { StateFrame } from '../../../library/kit';

/** ResizablePanelGroup · Controlled — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/resizable-example.tsx, тег shadcn@4.21.0). */
export default function ResizableControlledState() {
  return (
    <StateFrame muted>
      <ResizableControlled />
    </StateFrame>
  );
}
