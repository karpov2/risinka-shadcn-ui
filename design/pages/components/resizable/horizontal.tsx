/**
 * @section ResizablePanelGroup
 * @title Horizontal
 */
import { ResizableHorizontal } from '../../../../src/examples/resizable-example';
import { StateFrame } from '../../../library/kit';

/** ResizablePanelGroup · Horizontal — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/resizable-example.tsx, тег shadcn@4.21.0). */
export default function ResizableHorizontalState() {
  return (
    <StateFrame muted>
      <ResizableHorizontal />
    </StateFrame>
  );
}
