/**
 * @section Badge
 * @title Variants
 */
import { BadgeVariants } from '../../../../src/examples/badge-example';
import { StateFrame } from '../../../library/kit';

/** Badge · Variants — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/badge-example.tsx, тег shadcn@4.21.0). */
export default function BadgeVariantsState() {
  return (
    <StateFrame muted>
      <BadgeVariants />
    </StateFrame>
  );
}
