/**
 * @section NativeSelect
 * @title Sizes
 */
import { NativeSelectSizes } from '../../../../src/examples/native-select-example';
import { StateFrame } from '../../../library/kit';

/** NativeSelect · Sizes — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/native-select-example.tsx, тег shadcn@4.21.0). */
export default function NativeSelectSizesState() {
  return (
    <StateFrame muted>
      <NativeSelectSizes />
    </StateFrame>
  );
}
