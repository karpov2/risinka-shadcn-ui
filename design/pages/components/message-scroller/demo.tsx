/**
 * @section MessageScroller
 * @title Демо
 */
import { MessageScrollerDemo } from '../../../../src/demos/message-scroller-demo';
import { StateFrame } from '../../../library/kit';

/** MessageScroller · Демо — демо shadcn/ui (своё демо Risinka). */
export default function MessageScrollerDemoState() {
  return (
    <StateFrame>
      <MessageScrollerDemo />
    </StateFrame>
  );
}
