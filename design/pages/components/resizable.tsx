/**
 * @section Раскладка
 * @title ResizablePanelGroup
 */
import ResizableDemo from '../../../src/demos/resizable-demo';
import { ComponentTile } from '../../library/kit';

/** ResizablePanelGroup — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/resizable-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function ResizablePanelGroupTile() {
  return (
    <ComponentTile>
      <ResizableDemo />
    </ComponentTile>
  );
}
