/**
 * @section Поля и формы
 * @title Input
 */
import { InputDemo } from '../../../src/demos/input-demo';
import { ComponentTile } from '../../library/kit';

/** Input — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/input-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function InputTile() {
  return (
    <ComponentTile>
      <InputDemo />
    </ComponentTile>
  );
}
