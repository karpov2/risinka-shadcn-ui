/**
 * @section Select
 * @title Inline with Input & NativeSelect
 */
import { SelectInline } from '../../../../src/examples/select-example';
import { StateFrame } from '../../../library/kit';

/** Select · Inline with Input & NativeSelect — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/select-example.tsx, тег shadcn@4.21.0). */
export default function SelectInlineState() {
  return (
    <StateFrame muted>
      <SelectInline />
    </StateFrame>
  );
}
