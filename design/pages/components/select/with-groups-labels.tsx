/**
 * @section Select
 * @title With Groups & Labels
 */
import { SelectWithGroups } from '../../../../src/examples/select-example';
import { StateFrame } from '../../../library/kit';

/** Select · With Groups & Labels — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/select-example.tsx, тег shadcn@4.21.0). */
export default function SelectWithGroupsState() {
  return (
    <StateFrame muted>
      <SelectWithGroups />
    </StateFrame>
  );
}
