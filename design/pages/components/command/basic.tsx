/**
 * @section Command
 * @title Basic
 */
import { CommandBasic } from '../../../../src/examples/command-example';
import { StateFrame } from '../../../library/kit';

/** Command · Basic — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/command-example.tsx, тег shadcn@4.21.0). */
export default function CommandBasicState() {
  return (
    <StateFrame muted>
      <CommandBasic />
    </StateFrame>
  );
}
