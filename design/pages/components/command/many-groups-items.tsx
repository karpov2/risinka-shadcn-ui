/**
 * @section Command
 * @title Many Groups & Items
 */
import { CommandManyItems } from '../../../../src/examples/command-example';
import { StateFrame } from '../../../library/kit';

/** Command · Many Groups & Items — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/command-example.tsx, тег shadcn@4.21.0). */
export default function CommandManyItemsState() {
  return (
    <StateFrame muted>
      <CommandManyItems />
    </StateFrame>
  );
}
