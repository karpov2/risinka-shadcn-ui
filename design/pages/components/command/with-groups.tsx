/**
 * @section Command
 * @title With Groups
 */
import { CommandWithGroups } from '../../../../src/examples/command-example';
import { StateFrame } from '../../../library/kit';

/** Command · With Groups — сводный пример shadcn/ui (MIT, apps/v4/registry/bases/base/examples/command-example.tsx, тег shadcn@4.21.0). */
export default function CommandWithGroupsState() {
  return (
    <StateFrame muted>
      <CommandWithGroups />
    </StateFrame>
  );
}
