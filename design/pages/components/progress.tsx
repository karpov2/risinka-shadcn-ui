/**
 * @section Обратная связь
 * @title Progress
 */
import ProgressDemo from '../../../src/demos/progress-demo';
import { ComponentTile } from '../../library/kit';

/** Progress — плитка сводки shadcn/ui: официальное демо (MIT, apps/v4/examples/base/progress-demo.tsx, тег shadcn@4.21.0). Все варианты — на странице компонента. */
export default function ProgressTile() {
  return (
    <ComponentTile>
      <ProgressDemo />
    </ComponentTile>
  );
}
